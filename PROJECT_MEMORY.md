# Memoria del proyecto TRENCHES
- 2026-09-25l (COMPARTIR CHARTS desplegado y verificado — commits `e114dc7` + bump v20260925-11; gh-pages `c3c97ba`): el botón "Copiar enlace" del terminal pasa a **share completo**: captura el canvas REAL de Lightweight Charts 4.1 (`chart.takeScreenshot()`), compone banner 1000px (barra superior con TRENCHES/chain/precio/24h reales del terminal, chart, pie "inusaur.online · datos reales, sin custodia") y: móvil → `navigator.share({files:[png]})` con link en el texto; desktop → descarga PNG `trenches-<sym>-<chain>.png` + copia el deep link; sin chart renderizado o share rechazado → degradación honesta a link-only (nunca imagen inventada). QA Playwright producción `output/qa-share.cjs` **5/5**: PNG con cabecera mágica validada, filename correcto, estado "Imagen descargada + enlace copiado", 0 pageerrors. TRAMPAS: (1) los imports relativos de módulos (terminal-view.js sin ?v=) NO se invalidan con el bump de app.html — el CDN sirvió el módulo viejo y el QA ejecutó el handler anterior; verificar SIEMPRE con curl + query param y en el QA forzar `page.addInitScript` que reescriba fetch del módulo; (2) el evento download de Playwright para blob: URLs requiere esperar antes del revoke (usado 4s). Prioridad restante del operador: pulir el terminal de trading self-custody.
- 2026-09-25k (PIVOTE — feedback del operador + landing de conversión): el operador manifestó que no entendía la pestaña 🧩 Pairs ni le veía valor en la web. DECISIÓN (preguntada con ask_user): (1) OCULTAR Pairs de la nav desktop — commit `8ad2e07`, gh-pages `821331f` v20260925-9; el deep link #pairs= sigue vivo y el backend sigue acumulando snapshots sin coste; reactivar = restaurar el botón data-nav="pairs". (2) PIVOTAR a: Landing y adquisición + Compartir charts + Terminal de trading. (3) LANDING ejecutada (commits `bda5c8a`, `ec9e6b9`, `0774aa2`; gh-pages `2a59c26`→`c821dc8` v20260925-10): hero con UN solo CTA primario ("Crear cuenta gratis y operar →") + botón ghost de instalar (antes 3 CTAs primarios compitiendo + invite-link); nav de 2 CTAs a 1; ELIMINADOS los relicarios muertos de la beta: modal de código de invitación (handleAccessCodeSubmit/openInviteModal/closeInviteModal), modal fake de success de waitlist ("Batch 01", "posición #1,421 en la cola", shareOnX/copyInviteLink), handleWaitlistSubmit/loadWaitlistCount y CSS .waitlist-form/.invite-link — 273 líneas de teatro deshonesto sin formulario vivo que los invocara; botones BUY/SELL del mockup del hero ahora enlazan a app.html real en vez de abrir el modal; overflow móvil real corregido (hero-glow width:min(760px,100vw) — lección: overflow-x:hidden en html/body recorta el SCROLL pero scrollWidth sigue reportando el elemento ancho; el QA debe medir el elemento, no confiar en el clip). QA Playwright output/qa-landing.cjs 9/9 (1 CTA, sin teatro, mockup feed con datos reales CoinGecko, BUY/SELL→app, móvil 390 scrollW=390, 0 pageerrors). TRAMPA: tras push a gh-pages, Playwright sirvió HTML en caché — verificar con curl + query param y reintentar. API_BASE eliminado de index.html (ya no se usa). Siguientes prioridades del operador: compartir charts (imagen+link para X/Discord) y pulir el terminal de trading self-custody.
- 2026-09-25j (FRACTIONALIZATION — maquinaria honesta construida y desplegada; commits `18ef9f6` + `a3e95ba` bump v20260925-8; gh-pages `0ad9051`): INVESTIGACIÓN PREVIA (web_search 2026-09-25): NO existe en vivo ningún protocolo de fractionalización NFT en Solana con API pública — Solvent discontinuado, Droplets (freeze/mint) cerrado; lo que queda son builds custom sin vaults públicos. Decisión según regla de oro: el registro sale VACÍO y documentado, jamás con sustitutos sintéticos. (1) `pair-fractional.ts`: FractionalAsset (underlyingCollection, protocol, vault, sharesMint obligatorios), FRACTIONAL_ASSETS=[] con la evidencia de la sonda y checklist de re-activación en 4 pasos (verificar programa on-chain → endpoint público de estado del vault → registrar asset+reader → solo entonces DNA con vault no-null y Teleport con capacidad real); hasVerifiedVault/verifiedFractional/fractionalDnaFields (unverified → vault null + restriction "FRACTIONAL: sin protocolo verificado en vivo — sin vault, sin backing, sin ejecución"). (2) pair-engine: PairAsset.underlyingCollection?, PairDNA.vault ensanchado a string|null y fractionalization a string (buildDNA sigue devolviendo none/null — compatible). (3) Teleport: patas fractional_nft sin vault verificado → NO_ROUTE con razón exacta ("fractionalization sin protocolo verificado en vivo — no hay vault ni backing real") tanto en base como en quote. (4) UI Pair DNA: filas FRACTIONAL (protocol id) y VAULT (dirección o "null — sin vault verificado"). (5) 450 tests (+4 en pair-fractional.test.ts: registro vacío, DNA fields, legs no-fractional limpios, Teleport refusal base+quote). Producción verificada: dominio sirve v8, VAULT row en pairs.js servida, health live, mad_lads/wrapped-sol sigue FLOOR_READY. QA Playwright 4/4 (FRACTIONAL+VAULT visibles, 0 pageerrors, sin overflow). Spec Universal Pairs: fases motor/teleport/floor/rotate/fractionalization cerradas; restan vaults/Pair Orders (necesitan un protocolo real que fractionalize).
- 2026-09-25i (ROTATE UX desplegada y verificada en producción — commits `bef0964` feature + `4a52845` bump v20260925-7; gh-pages `9dca133`): (1) Terminal de par gana botón "⇄ Rotar QUOTE→BASE" (reabre el par inverso con `rotatePair()` → `openPair(inverso,{force:true})`, deep link se actualiza) y el panel Teleport gana "⇄ Inversa" (cota la ruta contraria con `checkRoute({reverse:true})` sin salir de la vista). (2) Teleport: la pata quote-NFT (par inverso SOL/MAD) ahora nombra el bloqueo real — "venta de NFT no habilitada todavía (comprar/vender el floor exige firmar el programa on-chain de Magic Eden)" — en vez del genérico "sin mercado ejecutable". (3) 446 tests (+1: SOL/MAD NO_ROUTE con razón de venta NFT), typecheck/build OK, smoke local del estado inverso verificado. (4) PRODUCCIÓN: Fly deploy OK, dominio sirve v20260925-7, API inversa → NO_ROUTE con razón nombrada en live. QA Playwright `output/qa-rotate.cjs` **13/13**: botones visibles, inversa cotiza sin navegar, deep link rota a SOL/MAD, el botón Rotar invierte su etiqueta (MAD→SOL), doble rotación vuelve al original, móvil 390 rota sin overflow, 0 pageerrors. Siguientes fases del spec: fractionalization (PairAsset fractional_nft con vault verificado), vaults/Pair Orders.
- 2026-09-25h (FLOOR LIQUIDITY con guard de rareza — FLOOR_READY, commit `13272ce`): fase siguiente del spec ejecutada. (1) Sondas keyless: `v2/collections/:symbol/listings` devuelve listados REALES con rareza embebida (howrare/moonrank/meInstant, nombre, atributos, vendedor, precio en lamports; verificado en vivo con mad_lads). COMPRA del floor NO habilitable aún: exige firmar el programa on-chain de Magic Eden (gateado por su API key; confirmado por docs comunitarias + endpoints de exec 404/"no healthy upstream") — el estado intermedio es honesto y NUNCA se presenta como ejecutable. (2) `src/pairs/floor-liquidity.ts` nuevo: FloorLiquidityService pagina cheapest-first (page 20, máx 5 páginas, TTL 2 min, tope 25 req/min) y aplica el GUARD DE RAREZA: excluye las piezas del top-N más raras (`rank <= maxRank`, default DEFAULT_MAX_RANK=250; rank bajo = raro) — una ganga rara jamás hace de floor; rank null pasa (no se inventa). Floor real = mint, priceSol, priceLamports, name, rarityRank+raritySource, listingUrl magiceden.io/item-details. Sin listados o todo excluido → floor null. (3) Teleport: `findRoute(base, quote, amountIn, maxRankRaw)` con tercer estado **FLOOR_READY** (hops 1, leg kind "floor_ready" con error nombrando el bloqueo de ejecución, campo `floor` = snapshot real). Sin floor que supere el guard → NO_ROUTE con razón nombrada ("N de M listados son piezas del top-N más raras (guard de rareza)..."); sin proveedor → NO_ROUTE honesto. (4) API: GET /api/pairs/route acepta `maxRank` (clamp 1..1e6, basura → default). (5) UI pairs.js checkRoute: FLOOR_READY muestra el listing real (nombre, floor SOL, rank+fuented, listados observados/excluidos, link a Magic Eden) con aviso "leg NUNCA ejecutable". (6) VERIFICADO LOCAL con datos reales: mad_lads/wrapped-sol → FLOOR_READY con Mad Lads #3820 @9.5137 SOL rank #3257 (meInstant); con maxRank=5000 la #3820 queda excluida y el floor pasa a #4837 rank #7770 (semántica exacta del guard); wrapped-sol/usd-coin → ROUTABLE Jupiter intacto (1 SOL→116.27 USDC); bitcoin → NO_ROUTE honesto. QA real vía /api/pairs/route en servidor local (dist), 0 pageerrors en la capa API. (7) TRAMPA DE SEMÁNTICA DEL GUARD: primero lo codifiqué como "rank > maxRank excluida" (inverted) y los tests lo cazaron; la regla correcta es `rank <= maxRank → excluida` porque top-N raras son las de rank bajo. clampMaxRank exporta 1..1e6. (8) Tests 445 app (+9: floor-liquidity.test.ts con fetch stubbed en forma real del payload ME, paginación al pasar el guard, guard excluye top-N, rank null pasa, colección vacía, 503 upstream; teleport FLOOR_READY con listing real, guard→NO_ROUTE con razón top-N, sin proveedor→NO_ROUTE). Typecheck/build OK. (9) DESPLEGADO Y VERIFICADO EN PRODUCCIÓN: commit `7602e00` (bump v20260925-6, 27 refs), Fly deploy OK (health {ok,live}), `/api/pairs/route?q=mad_lads/wrapped-sol` en prod → FLOOR_READY real (Mad Lads #4591 @9.588 SOL rank #2816 meInstant); gh-pages `06b1f3c` v20260925-6 (app.html + js/pairs.js, dominio verificado con curl). QA Playwright producción 11/11 (`output/qa-floor-route.cjs`, `waitUntil: domcontentloaded` — networkidle nunca llega porque la app sondea APIs): desktop 1280 y móvil 390 renderizan FLOOR READY con listing real + rank + link a Magic Eden + aviso "NUNCA ejecutable", 0 overflow, 0 pageerrors. Siguientes fases del spec: fractionalization, ROTATE UX sobre el routing existente.
- 2026-09-25g (LIQUIDITY TELEPORT v1 — routing ejecutable con honestidad de estado, desplegado): commit `5129a00`, web `c8de532` v20260925-5. (1) `src/pairs/teleport.ts`: registro EXECUTABLE_TOKENS (wrapped-sol, usd-coin en Solana; ethereum→WETH en Ethereum; BTC analítico-only — sin registro NO se ejecuta), grafo hub-based (hubs: usd-coin, wrapped-sol, ethereum) con profundidad MÁX 2 hops, cotización de legs REAL vía TradingEngine.getQuote (Jupiter/0x/Li.Fi — userId 0, type swap, price-only; la ejecución real sigue exclusivamente en self-custody prepare/submit), leg2 del hub se cotiza con el amountOut REAL de leg1, totalImpact = suma de impactos por leg. Estados honestos: ROUTABLE (legs con venue/impacto/salida) | NO_ROUTE (razón nombrada: "MAD: ejecución de floor NFT no habilitada todavía" / asset sin mercado conocido / sin hubs encadenables). (2) API GET /api/pairs/route?q=base/quote&amountIn=<units> (amountIn regex ^[0-9]{1,30}$, 400 si inválido, 404 asset desconocido). (3) UI: panel 🌀 Liquidity Teleport en el terminal del par con botón Chequear ruta — ROUTABLE lista legs numerados con venue+impacto+salida final y aviso "cotización solo informativa, sin sesión ni firma"; NO_ROUTE explica la razón. (4) VERIFICADO EN PRODUCCIÓN: wrapped-sol/usd-coin → ROUTABLE 1 leg Jupiter 1 SOL→117.71 USDC impacto 0.000% (cotización REAL en vivo); mad_lads/wrapped-sol → NO_ROUTE honesto. QA navegador ambos estados + móvil 390 limpio + 0 pageerrors. 436 tests (+6: registro, NO_ROUTE por NFT/BTC con razón, leg directa con campos reales, encadenamiento hub amountOut→amountIn con intento fallido contabilizado — trampa: el intento directo fallido también entra en el log de llamadas). Siguiente fase natural del spec: NFT floor liquidity con guard de rareza (convierte legs route_pending en ejecutables), fractionalization, ROTATE UX sobre el routing existente.
- 2026-09-25f (UNIVERSAL PAIRS v1 CONSTRUIDA Y DESPLEGADA — capa de valor relativo): commits `c483cd9` (feature), `6f12eb8` (fix CG), `99a5f8d` (fix self-compare); web gh-pages `24f18da`→`b45b5f1` v20260925-4. (1) MOTOR `src/pairs/pair-engine.ts` puro: PairAsset (token|nft_collection|fractional_nft|index|vault|tokenized_asset), PairDNA (buildDNA: modo, oráculo, backing "none", vault null, restricciones honestas), ratioSeries (alinea timestamps, null si falta pata), relativeSeries (re-escala ambas patas a 100 al inicio; null sin precio inicial — NUNCA inventa 100s), changePct, currentRatio, pairMomentum (breakout con umbrales visibles: relativo ≥+5pp Y ratio ≥+3%), pairIntelligence (gana/pierde vs benchmarks en pp; sin dato = omitido). (2) PRECIOS `pair-prices.ts`: Magic Eden `v2/collections/{sym}/stats` KEYLESS (floorPrice lamports→SOL, listed, avg24h, vol7d; TTL 10min, tope 25/min — 5 colecciones ≈0.5 req/min), CoinGecko `coins/markets` batch (TTL 2min); snapshots acumulativos en tabla `pair_snapshots` (PK asset_id+ts, upsert, índice), loop de 5 min en server.start() con pasada inmediata. Colecciones: mad_lads/okay_bears/claynosaurz/famous_fox_federation/degenerate_ape_academy (todas verificadas live). Tokens: SOL/ETH/BTC/USDC/NVDAX (nvidia-xstock REAL)/ENS/PENGU/FOX. (3) SERVICIO `pair-service.ts`: pares DIRECTOS (floor NFT/SOL observado) + SINTÉTICOS (ratio USD/USD de dos precios reales, ANALÍTICOS por definición); usdSeries convierte floor SOL→USD alineando con snapshot SOL/USD del mismo ts (timestamps sin SOL/USD → null); directPrice = floor observado incluso sin histórico. (4) API: GET /api/pairs?kind=trending|nft-token|synthetic|relative-movers|relative-breakout, /api/pairs/resolve?q=MAD/SOL (resuelve por id o símbolo), /api/pairs/:id/detail (series+DNA+patas+nft stats+intelligence), /api/pairs/assets. (5) UI `site/js/pairs.js`: vista 🧩 Pairs con descubrimiento (5 filtros, badges DIRECTO/SINTÉTICO, cards con ratio/floor/relativo/breakout), terminal de par (stats header, chart Lightweight Charts RELATIVO base/quote/relative + PAIR, Pair DNA, cards de patas con floor/listados/volumen, Intelligence gana/pierde), matriz relativa 8×8 (celdas verdes/rojas por intensidad), deep link #pairs=base%2Fquote. (6) TRAMPAS: CoinGecko RETIRÓ el coin id `wrapped-sol` (404 en /coins) — el SOL/USD vive bajo `solana`; el par conserva el slug estable. La inteligencia jamás compara un asset consigo mismo. ratioSeries solo se expone con ≥2 ratios reales. QA producción verificado: pares con ratios reales (MAD/ETH 0.3931, ETH/SOL 22.89), chart RELATIVO renderiza, matriz con datos, móvil 390 sin overflow, 0 pageerrors. 430 tests (+13). Límites honestos actuales: histórico relativo crece con snapshots (2 puntos al día 1), ejecución/routing (Liquidity Teleport), fractionalization, vaults y Pair Orders quedan para fases siguientes del spec.
- 2026-09-25d (PERFIL SOCIAL "Tu Red" desplegado y verificado con datos reales): sección en la vista Profile (portfolio.js `appendSocial`, commit `ac05565`): panel con contadores (seguidores según followers_count del perfil + siguiendo según lista real), chips de actores (avatar real si avatarUrl, iniciales si no; displayName, @handle, PnL realizado si existe) con escHtml en todo, máx 24 chips por sección, fallas del grafo no tumban la vista (Promise.all con catch independientes), arrays following/followers inicializados a []. ApiClient nuevos: getMyFollowing/getMyFollowers (`/api/users/me/following|followers`). Web gh-pages `7d11bed` v20260925-2 (app.html, js/api.js, js/portfolio.js). Verificación con identidad efímera + Playwright (inyección de raidos_key en localStorage antes de cargar): "🧑‍🤝‍🧑 Tu Red / 1 seguidor · 1 siguiendo" con chips reales; QA móvil post-deploy 8/8 sin overflow; único 503 = /api/market/reference (degradación GT conocida, cubierta por fallback del navegador). TRAMPA DEL FLUJO DE PUBLICACIÓN: copiar app.html apuntando a versiones nuevas pero olvidando copiar el JS versionado deja el API-call vivo con render viejo (el QA anónimo no lo detecta; siempre verificar el JS servido con curl + el flujo autenticado con identidad efímera).
- 2026-09-25c (FOLLOWS DESPLEGADO Y CERTIFICADO E2E EN PRODUCCIÓN): (1) Fly deploy OK desde la raíz (flyctl en C:/Users/Usuario/.fly/bin/flyctl.exe), health {ok,live}; producción: POST/GET follow anónimo → 401, /api/users/1/followers → 200 {followers:[]}. (2) Web gh-pages nachoweb3/inusaur `0ea3650` v20260925-1 (4 ficheros reales: app.html, js/api.js, js/social.js, js/feed.js); verificado servido: banner v=20260925-1, social.js con trenches_following_ids, feed.js con followingIdsCache, api.js con followUser. (3) E2E REAL en producción `packages/app/scripts/qa-follows-e2e.mjs` → **PASS**: dos wallets efímeras ed25519 (node:crypto generateKeyPairSync; trampa: la firma debe ir en HEX — verify-login.ts hace Buffer.from(sig,"hex") — y la pubkey raw son los ÚLTIMOS 32 bytes del DER SPKI) → login self-serve → A sigue B → follow {created,followersCount:1} → idempotente (created:false) → /users/me/following resuelve → followers público ve al follower → unfollow {removed:true} → guards self-follow 400 / usuario fantasma 404. No toca dinero. (4) QA Playwright post-deploy: móvil 390 8/8 sin overflow, 0 errores consola (solo los 3 targets 36×36 conocidos); desktop feed filtro Siguiendo sin crash; leaderboard sin botones Follow porque el ranking aún no tiene traders con fills (estado honesto, no bug). Registrarse por email exige BOOTSTRAP_SECRET; wallet-login es el camino self-serve (challenge→firma→/api/auth/wallet).
- 2026-09-25b (FOLLOWS PERSISTENTES: grafo social server-side + filtro de feed por actor; commit `e2b8649`, pendiente deploy): cerrada la fila "Seguimiento persistido — tabla follows sin rutas; UI localStorage". (1) DB: `ensureProfile(userId)` (INSERT OR IGNORE, perfiles perezosos — el registro de usuario no crea perfil). (2) API: POST/DELETE/GET `/api/users/:id/follow` (auth; 400 self-follow, 404 usuario inexistente, created/removed idempotentes, followersCount devuelto), GET `/api/users/:id/followers|following` (públicos, forma canónica `socialActor`: handle `@x_handle` o `@trader_<id>`, displayName, avatarUrl, followersCount, pnl/winRate/trades). (3) Feed: `/api/feed?actorIds=1,2,...` (lista de ids, máx 100, basura→sin filtro, nunca 500) con nuevo filtro SQL `actor_id IN (...)` en getFeed. (4) UI social.js: botones Follow/Siguiendo usan userId con sesión (optimista + revert en fallo, caché `trenches_following_ids`) y fallback local anónimo por handle sin sesión; sync desde `/api/users/me/following` (el servidor resuelve "me" → usuario autenticado en followers/following, 401 anónimo, testeado; commit `a640215`). (5) feed.js: filtro "Siguiendo" autenticado por `actorId` server-side (mata el mismatch @trader_<id> vs x_handle que hacía el filtro local siempre vacío); anónimo mantiene el grafo local. (6) Tests +5 (`tests/follows.test.ts`): 401/404/400, idempotencia follow/unfollow, forma canónica, actorIds; +mock de `isAuthenticated` en social-safety.test.ts. 417 tests app, typecheck/build OK, sintaxis site OK, smoke local sin errores. Bump de importmap aplicado (v20260925-1, 26 refs). Nota: POST /api/feed/post devuelve 201 (no 200).
- 2026-09-25 (QA de regesión post-deploys: móvil 8/8 limpio, desktop intacto, señales OK en vivo): Playwright sobre PRODUCCIÓN (output/mobile-qa.cjs con QA_BASE=https://inusaur.online; trampa de local: el estático :8931 no hace proxy /api, en local el frontend llama same-origin y TODO da 404 — 18 rutas; en producción mismo QA da 0 errores de consola). Móvil 390×844: 8/8 vistas scrollW=390, 0 overflow; 3 targets 36×36/38×36 restantes (umbral aceptable, no regresión). Desktop 1280: 8/8 sin overflow, 0 pageerrors. Pestaña ⚡ Señales en vivo: tabs correctas (Traders/CT Calls/Señales/Siguiendo), estado vacío anónimo con copy de onboarding correcto, screenshot output/qa-signals-anon.png. API verificada con curl: PATCH /api/copy/signals/:id → 401 sin auth (protección OK), GET ?status=bogus → 401 antes de validar (auth-first OK), health {ok,live}. Suite: 412 tests app pasando en local. Servidor local QA: `node dist/api/main.js` (NO dist/server.js) con APP_MODE=live PORT=8930 DB_PATH=$TEMP/qa-api.db SITE_DIR=../../site desde packages/app. Sin cambios de código en esta sesión; todo el trabajo fue verificación.
- 2026-09-24e (UI MÓVIL corregida y desplegada, auditoría con Playwright): el usuario reportó móvil sin optimizar. Auditoría con Playwright (iPhone 14, 390×844, touch) sobre script reutilizable `output/mobile-qa.cjs` (QA_BASE parametrizable: producción o servidor local `output/static-server.cjs` :8931 que sirve site/ sin deploy): las 8 vistas tenían scrollWidth 464 > 390 (la página entera se desplazaba lateral) y ~25 targets táctiles de 27px por vista. Causas y fixes (commit `2068100`, gh-pages `8e0a87f` v20260924-3): (1) header actions (search 230px + select + botones) no cabía y no hacía wrap → header ahora flex-wrap con 2 filas (marca / acciones+búsqueda full-width order:10, font 16px anti-zoom iOS) y "Instalar app"/⚙ ocultos en móvil; (2) REGLA PERDIDA: al reescribir el media query se dropeó `.feed-grid { grid-template-columns: 1fr !important }` → el feed colapsaba a ~100px (inline 1fr 340px ganaba) — restaurada; (3) pill-tabs y .btn-sm con min 36px, fila de acciones del feed (like/repost/share spans de 12px) ahora flex:1 min-height 36px con clase .feed-actions, link del footer 36px; (4) trampa de auditoría: los ✕ de modales cerrados reportan opacity:1 (el wrapper tiene opacity:0) — el chequeo debe caminar ancestros. Resultado verificado EN PRODUCCIÓN: 8/8 vistas scrollWidth=390, 0 targets <36px (quedan 3 de 36×36 aceptables), desktop 1280px intacto (hdrH 88). agent-browser quedó inestable en esta máquina (daemon cuelga las llamadas; matar PID y retry no alcanza) — usar Playwright de C:/Users/Usuario/node_modules. Lección de método: probar SIEMPRE en servidor local antes de publicar (el board de trade da error esperado sin API).
- 2026-09-24d (DESPLEGADO: ciclo de vida de señales + crash de migración corregido): (1) Commit `9e7e2e6` (feature) y `d73933a` (fix). (2) PRIMER deploy a Fly FALLÓ con "app appears to be crashing": el nuevo índice idx_copy_signals_user_status estaba dentro del bloque CREATE TABLE (no-op sobre DB existente) y referenciaba la columna status que solo el ALTER TABLE posterior crea — boot moría con "no such column: status". LECCIÓN: en SQLite toda columna nueva debe existir ANTES de cualquier CREATE INDEX que la use; el patrón de migración incremental debe cubrir también los índices. Fix: índice movido tras la migración + test de regresión que abre un esquema pre-migración real (construido con better-sqlite3 crudo) y verifica boot + ciclo de vida in-place (415 tests). (3) Segundo deploy OK, health {ok,live}, PATCH/signals rutas vivas (401 sin auth), leaderboard 200. (4) Web gh-pages `5aa7952` v20260924-2 publicada en nachoweb3/inusaur (solo app.html + js/copy.js con diff real; resto era ruido CRLF restaurado con git checkout). Verificado público: dominio sirve v=20260924-2, copy.js nuevo servido (openSignal/resolveSignal/toggleSignalsHistory), 200. En vivo: pestaña ⚡ Señales = cola accionable, botón ✕ descarta, 1-TAP marca done antes de abrir el terminal, toggle historial con chips.
- 2026-09-24c (ciclo de vida de señales copy: cola accionable + historial): cerrado el hueco del pipeline copy — las señales entregadas eran eternamente pendientes. (1) DB: `copy_signals` gana `status` ('pending'|'done'|'dismissed', default pending) + `resolved_at`, migración idempotente por pragma_table_info y nuevo índice (user_id,status,ts DESC). (2) API: GET /api/copy/signals pasa a cola accionable (default status=pending, acepta ?status=all|done|dismissed, 400 en valor inválido) y nuevo PATCH /api/copy/signals/:id con {status: done|dismissed} — scoped al dueño, sólo pending→terminal (replay/ajeno devuelve 409, nunca 500 ni filtración). (3) UI copy.js: pestaña Señales con botón ✕ descartar, toggle pendientes/historial con chips de estado, y 1-TAP ahora marca done ANTES de abrir el terminal (openSignal; si ya estaba resuelta abre igual, sin resurrect). (4) Tests +4 en social-engine.test.ts (cola default, historial, terminal no voltea, propiedad por usuario; usuarios de prueba creados directo en DB con generateApiKey — el userId de register es timestamp*1000+random, no secuencial). 411 tests app (+4), typecheck/build app+core OK, sintaxis copy.js OK, assets bump v=20260924-2 (26 refs). Pendiente deploy Fly + gh-pages cuando el operador autorice.
- 2026-09-24b (Arc CERTIFICADO E2E en producción + fix fromAddress + integrator): (1) FIX `daccccd`: Li.Fi exige `fromAddress` en /v1/quote SIEMPRE (400 "required property 'fromAddress'" — los probes manuales siempre lo llevaban y el stub de tests no validaba params, por eso pasó el test anterior); ahora se manda taker real o DEFAULT_TAKER placeholder (patrón 0x). Lección: stubs de fetch deben validar querystring exigente. (2) E2E REAL en producción (wallet efímera ethers, login challenge→link→quote→prepare): quote 200 (buy=362856462839243, route=Kyberswap, agg=lifi), prepare 200 con sessionId, swap.to=Diamond 0xA407…d40b value=0x0 chainId=5042, approveTx con amount EXACTO 0xf4240=1 USDC, buyAmountMin=359227646161485 (slippage 1% aplicado por Li.Fi). Solo falta broadcast con fondos reales. (3) LIFI_INTEGRATOR=trenches staged y desplegado en Fly (quotes con integrator=200 OK). (4) La fee del integrador (LIFI_FEE_PCT) REQUIERE cuenta en portal.li.fi con fee wallet configurada — sin ella quotes con fee dan 400 code 1011 "Integrator not configured for collecting fees"; NO activar hasta que el operador registre la cuenta. (5) QA navegador: banner v20260924-1 en vivo, gate del terminal para arc pasa (mismas condiciones liveExecution+LIVE que ethereum/base). Registro por email en prod exige BOOTSTRAP_SECRET (wallet-login es el camino self-serve para tests E2E). 407 tests.
- 2026-09-24 (SWAPS EN ARC EN VIVO vía Li.Fi + QA copy trading): Probe read-only verificado: pool WETH/USDC de Arc con ~$30M vol24h, Li.Fi enruta (tool=kyberswap, transactionRequest completo to=Diamond 0xA407…d40b, value=0x0, gasLimit/gasPrice, estimate.toAmountMin, approvalAddress) — CRÍTICO: en Arc el USDC nativo es tratado por Li.Fi como ERC-20 normal (tx value 0x0 + approve aparte). Docs Li.Fi verificadas: la fee del integrador se descuenta del fromToken (pata de venta) → misma contabilidad que 0x, parseEvmTransferFill (deltas de Transfer owner-scoped) valida sin cambios y el tope de exposición diaria por pata USDC funciona igual. Implementado: (1) `getLiFiArcQuote` en engine.ts (GET li.quest/v1/quote, slippage fracción bps/10000, LIFI_INTEGRATOR/LIFI_FEE_PCT opcionales por env sin redeploy, TradeQuote.buyAmountMin nuevo); (2) prepareSelfCustodyTransaction emite `approveTx` exacta para el Diamond (erc20ApproveCalldata, selector 0x095ea7b3, amount=sellAmount sin allowances adivinadas) y site/js/trading.js firma approve→swap con nonces secuenciales (el mempool garantiza el orden; sin polling de allowance); (3) flip: arc en SELF_CUSTODY_CHAINS + /api/chains lo anuncia LIVE keyless con dexAggregator=lifi (el test de production-guards cambiado a arc NO requiere key — correcto); (4) platformFeeBps=0 persistido en sesiones arc (el fee 0.3% viaja dentro de la ruta vía LIFI_FEE_PCT, no vía receipt bps); (5) WETH de Arc en WELL_KNOWN_TOKENS, explorer de arc corregido en holders.ts (explorer.arc.io, estaba apuntando a testnet.arcscan.app), banner chainNotice actualizado, arc-route-watch.mjs ahora es chequeo de liveness (exit 0 = integración hecha). 406 tests (+11 en arc-lifi.test.ts: forma de quote, approve exacto, E2E HTTP sesión→submit→replay 409, 503 honesto, calldata, receipt settlement). Deploy Fly OK — `/api/chains` en prod: arc LIVE/lifi/live=true. Web gh-pages `5e29997` v20260924-1 (app.html + app.js + trading.js). QA en vivo con navegador (agent-browser): banner nuevo en vivo, capacidades LIVE vistas por el frontend, pestañas Copy OK (Traders lista 30 wallets con ratings honestos "NO RECOMENDADO PARA COPIAR"/"EVIDENCIA INSUFICIENTE", modal ratingDetail con razones legibles: win rate 8%, hold de segundos "imposible de copiar sin latencia de bot"; Señales estado vacío honesto; CT Calls degradación honesta sin TWITTERAPI_IO_KEY), pools arc 200 LIVE ~0.3-0.5s con 503s intermitentes por 429 de GT (conocido; fallback del navegador cubre). Pendiente del operador: TWITTERAPI_IO_KEY (CT calls), fondeo tesorería ~0.2 SOL, opcional LIFI_INTEGRATOR+LIFI_FEE_PCT=0.3 en fly secrets para cobrar fee en Arc (sin redeploy). Primer swap real en Arc aún no ejecutado (requiere usuario con wallet; flujo testado E2E offline + forma de tx verificada contra quote real).
- 2026-09-23d (COPY TRADING signal+1-tap + CT tracker estilo j7/Cheetah — construido y desplegado): Investigación: Cheetah (0.5%/trade, min $0.95, ratings opacos, semi-custodial) y j7tracker (CT tracker <200ms, listas curadas) — decisión del usuario: modelo SEÑAL + 1-TAP (no-custodial, el usuario firma cada trade), rating TRANSPARENTE propio, fee 0.3% sin mínimo. Implementado: (1) `src/social/rating.ts` — wallet-rating-v1 fórmula pública (pesos: expectativa 30%, PnL realizado 20%, muestra 15%, profit factor 15%, hold 10%, consistencia 5%, concentración 5%; contabilidad FIFO por lotes = cada round-trip con su PnL/hold; veredictos honestos: <8 trades o <2 cerradas = SIN SCORE, trampa del 70% win rate detectada, scalping <5min no copiable, concentración >70% flag). (2) `src/social/sources.ts` — harvest de trades desde pools ya indexadas (market.trades, misma cuota/caché) + tweets de twitterapi.io (~$0.15/1k, requiere TWITTERAPI_IO_KEY; sin key = degradación honesta []) + extracción de señales SOLO con dirección on-chain resoluble en el catálogo (nunca cashtags). (3) DB: observed_trades (PK id → idempotente), ct_calls (UNIQUE tweet+token), copy_subscriptions (UNIQUE user+chain+wallet; chain='ct' + wallet=@handle para canal CT), copy_signals (fan-out con tope por trade y mirror_sells). (4) `src/social/engine.ts` — pass acotada cada 60s en server.start() (harvest→fan-out→ratings→precios CT), SocialEngine en ApiServer. (5) API: GET /api/copy/leaderboard (público, expone fórmula), GET /api/copy/rating/:chain/:wallet (público), GET /api/copy/ct-calls (público), CRUD /api/copy/subscriptions (auth), GET /api/copy/signals (auth); router ahora soporta PATCH. (6) UI: view-copy + nav 🧬 Copy + site/js/copy.js (4 pestañas: Traders/CT Calls/Señales/Siguiendo; 1-tap abre TerminalView pre-armado; rating con razones legibles). 395 tests (+22), typecheck/build OK, deploy Fly OK (leaderboard 200, ct-calls 200, signals 401 sin auth ✓), web e4e65ef v20260923-5. Pendiente activar: TWITTERAPI_IO_KEY (fly secrets) para CT calls; el radar necesita horas de harvest para poblar el leaderboard.
- 2026-09-23c (COINGECKO key activa + Li.Fi tiene RUTAS REALES en Arc + brief de socios): `COINGECKO_API_KEY` (demo, header x-cg-demo-api-key) configurada en Fly — verificadas las 5 cadenas (arc/base/solana/ethereum/bsc) con /api/market/pools 200 LIVE, 20 pares reales cada una (Arc: pools uniswap-v3-arc reales). HALLAZGO: **Li.Fi devuelve rutas ejecutables intra-Arc** (quote /v1/quote 5042→5042 USDC→token con transactionRequest completo to/data/gasLimit, allowanceTarget 0xA407…d40b, tool "fly", fee fijo Li.Fi 0.25%, gas cobrado en USDC ✓) — 0x sigue sin rutas (404/401). Swaps en Arc posibles YA vía Li.Fi como ejecutor alternativo; flip = añadir "arc" a SELF_CUSTODY_CHAINS + integrar ejecutor Li.Fi (misma UX Permit2-like: usuario firma transactionRequest). arc-route-watch.mjs actualizado: CG onchain como primario (GT 429-proof), probe Li.Fi ahora exige quote REAL con transactionRequest (no solo listado) — exit 0 confirmado = señal de flip. Brief para socios al 50% en docs/marketing/partners-brief.md (claims solo verificados).
- 2026-09-23b (vigilancia de rutas Arc + banner honesto en UI; lanzamiento público): Registro ABIERTO (`OPEN_REGISTRATION=1` en Fly, kill-switch sin redeploy; endpoint /api/auth/access-code devuelve {access:"open"}; el gate modal era cosmético, login wallet/Google/X ya era self-serve). Branding T-rex completo (favicon/íconos PWA/og-image/mascota hero) regenerable con `site/tools/make-icons.ps1`. Tesorería 48S2…bFy8 con 0.00093 SOL — fondeo pendiente (~0.2 SOL); asistente `packages/app/scripts/setup-treasury.mjs --wait --yes` hace todo (espera depósito, crea ~60 ATAs de fee, importa TREASURY_KEYPAIR_BASE64 a Fly por stdin, clave nunca en argv/historial). Vigilancia Arc: `packages/app/scripts/arc-route-watch.mjs` sondea 0x (quote permit2 1 USDC→token real de pool GT) + Li.Fi (/v1/chains) — exit 0 = toca flip SELF_CUSTODY_CHAINS, exit 2 = GT 429/sin par, --loop para polling 6h. Banner honesto: div #chainNotice en app.html + App.chainNotice() en app.js — al seleccionar Arc muestra que datos son LIVE y swaps esperan rutas; web `9096377` (v20260923-4). Hilo de lanzamiento publicado en docs/marketing/launch-thread.md.
- 2026-09-23 (Arc mainnet habilitada como cadena de DATOS; swaps pendientes de agregador): Arc (L1 de Circle, gas en USDC nativo) live desde 2026-09-16. Commit `2a8896d` + web `1974fc7` (v20260923-3). Config corregida del stub viejo de testnet: chainId **5042** (sondeado on-chain eth_chainId→0x13b2 vía rpc.mainnet.arc.io), explorer https://explorer.arc.io, USDC nativo `0x3600…0000` con **6 decimales** (confirmado con pool real de GT; los docs mencionan 18 en display de wallets, el contrato es 6). GeckoTerminal indexa "arc" (networks/arc/pools 200 verificado) → añadido a NETWORKS de market/data.ts, MARKET_CHAINS del catálogo y filtros de UI (catalog-board.js, public-market.js). /api/chains expone arc con quotes:false/status:UNAVAILABLE honesto — 0x probe en 5042 devolvió 404 "no Route matched" el 2026-09-23 (ningún agregador EVM tiene rutas aún; 1inch/KyberSwap/Odos/LI.FI tampoco según TrustSwap del 14-sep). Activar swaps = añadir "arc" a SELF_CUSTODY_CHAINS cuando un proveedor publique soporte (flip de una línea + sondas). 373 tests, deploy Fly OK. Limitación operativa conocida: pools de Arc vía API dan 503 desde Fly por el 429 de GT en IP datacenter (igual que el resto de cadenas GT-only); el fallback CoinGecko /onchain necesita COINGECKO_API_KEY (pendiente, gratis) y el respaldo del navegador ya cubre al usuario final. Hilo de lanzamiento listo en docs/marketing/launch-thread.md (claims solo verificados: fee Solana y swaps Arc excluidos de las promesas).
- 2026-09-23 (tesorería PREPARADA, pendiente solo de fondeo ~en días): asistente `packages/app/scripts/setup-treasury.mjs` commiteado (commit feat(scripts)). Flujo del operador en cmd.exe (NO bash: su shell es cmd): (1) Phantom → export private key de 48S2…bFy8; (2) `set /p SOLANA_FEE_SECRET=...` en cmd; (3) `node scripts\setup-treasury.mjs --wait --yes` desde packages/app. El asistente: decodifica base58/JSON-array/base64 (bs58 disponible), verifica round-trip keypair, espera fondeo (polling 15s, umbral MIN_TREASURY_SOL=0.15), lanza solana-fee-atas.mjs --create y pasa TREASURY_KEYPAIR_BASE64 a `flyctl secrets import` por stdin (nunca en argv/historial/echo; flyctl en C:/Users/Usuario/.fly/bin/flyctl.exe, app raidos-api). Balance actual de la tesorería: 0.00093 SOL → necesita ~0.2 SOL (~$30). Tras fondear: fee Solana 0.3% fluye solo (caché ATA 5 min) y claims de rewards pagan USDC real. El usuario dice que fondeará en unos días; NO toca nada más hasta entonces. Pendiente también: COINGECKO_API_KEY (gratis) para los 429. Plataforma verificada hoy: health {ok,live}, web 200, registro abierto OPEN_REGISTRATION=1, branding T-rex en vivo.
- 2026-09-23 (branding T-rex en vivo): commit `50b3098` — favicon + iconos PWA + og/twitter images generados desde la mascota T-rex del operador con el script reutilizable `site/tools/make-icons.ps1 -Source <png>` (System.Drawing; genera icon-64/192/512, apple-touch 180, trex-mascot.jpg 1024 q90 para el hero —reemplaza trenches-logo.webp en index hero y modal de app— y og-image.jpg 1200x630; background negro puro, compatible con mix-blend-mode:screen). Metas og:/twitter: añadidas a index y app; imágenes absolutas a https://inusaur.online/assets/. Web gh-pages del repo **nachoweb3/inusaur** `3def872..65f8b64` versión 20260923-3 (9 ficheros: 2 HTML + 6 assets + tools/). Verificado público: icon-64 200 8.8KB, trex-mascot 200, og-image 200 image/jpeg, 4 metas sociales servidas, manifest apunta a los PNG regenerados. El source del arte quedó en $TEMP/trex-source.png (efímero); regenerar = volver a pasar el script con el PNG original.
- 2026-09-23 (LANZAMIENTO PÚBLICO activado): commit `874b1d8` — registro abierto con kill-switch `OPEN_REGISTRATION` (leído por request; =1 acepta cualquier código en /api/auth/access-code con access:"open", apagado restaura la lista beta; tests +2). Login wallet/Google/X ya era self-serve; el gate era cosmético. Landing y gate reescritos a modo lanzamiento (badge ● LIVE, CTA "Crear cuenta gratis y operar", waitlist sustituida por acceso directo; el único "CLOSED BETA" restante es un comentario HTML invisible). API Fly redesplegada con `OPEN_REGISTRATION=1` activado vía --stage + deploy. Web gh-pages del repo **nachoweb3/inusaur** (NO raidos;gh-pages no existe ahí): `0aeab69..3def872` versión 20260923-2 (ficheros: index.html, app.html; CNAME/.nojekyll intactos; workflow implícito de Pages, no rama desde el repo de código). Verificado público: health {ok,mode:live}, POST access-code con código arbitrario → 200 access:open, dominio sirve copy de lanzamiento. Dónde publicar la web: clonar nachoweb3/inusaur gh-pages (procedimiento $TEMP/ghpages-publish). Pendiente del operador (dinero, no código): exportar key de tesorería 48S2…bFy8 de Phantom → `fly secrets set TREASURY_KEYPAIR_BASE64`, fondear ~0.2 SOL, `node scripts/solana-fee-atas.mjs --create` para que el fee 0.3% de Solana fluya (EVM ya cobra), rewards ya ACTIVOS por defecto (cashback 10%+referidos 10%, MIN_CLAIM $5, editable en DB sin redeploy).
- 2026-09-23 (publicación verificada): commit `6c9b3ab` (pool real + rewards payout on-chain). API redesplegada en Fly `deployment-01M36KAVAXS01EJK5P5PZQBZBE`, máquina 84ed23ea636178 healthy. Web publicada en gh-pages `0aeab69` versión 20260923-1 (4 ficheros reales: app.html, js/api.js, js/markets.js, js/rewards.js; resto era ruido CRLF que se restauró antes del commit). Verificación pública: health 200 live, /api/launches/1/pool responde honesto (404 sin pool), recovery de sesiones 401 sin auth, dominio sirve v=20260923-1 (25 refs en app.html), rewards.js trae "CLAIM ON-CHAIN", markets.js trae "Operar en la pool", sw.js/manifest 200, CORS OK para inusaur.online. flyctl estaba en C:/Users/Usuario/.fly/bin/flyctl.exe (no en PATH). El deploy de Fly se ejecuta desde la RAÍZ del repo (fly.toml espera el Dockerfile raíz); desde packages/app falla. POOL_EXECUTION_ENABLED y TREASURY_KEYPAIR_BASE64 siguen SIN configurar: el panel de pool se activa solo cuando el operador fondee y arme los secrets.
- 2026-09-23 (pool post-graduación y claim de rewards ahora REALES, pendiente solo de activación con secrets): desbloqueados los dos últimos superficies bloqueadas "para siempre".
  - **Pool AMM (launch-amm.ts)**: eliminado `requireAuditedPoolExecution` (503 permanente). Nuevo flujo real: prepare construye tx sin firmar + lee reservas y guarda sesión durable exactly-once en `self_custody_sessions` (id = hash del mensaje sin firmar, kind "pool-swap", con quoteReserves); submit consume la sesión una vez, re-verifica byte a byte que el mensaje firmado = preparado, re-chequea reservas contra la cita con tope de drift 2% (`priceDrift`/`POOL_MAX_PRICE_DRIFT`, error STALE_QUOTE→409), co-firma la pool tras journalizar la firma ANTES del broadcast (`computeSignatureFor` extrae la firma del fee payer del wire format con compact-u16 real — el primer bloque de 64 bytes tras el conteo de firmas es la firma de la tx), broadcast + confirm + `recordPoolSwap`. Recuperación: `GET /api/launches/:id/pool/sessions/:sessionId` resuelve prepared/not_broadcast/unknown/pending/failed/confirmed contra el RPC (idempotente vía `getPoolSwapBySignature`). Armado con `POOL_EXECUTION_ENABLED=1` (+ live mode + kill switch global + `LAUNCH_POOL_KEYPAIR_BASE64`). Snapshot `/pool` ahora expone `executionAvailable` honesto y `feeBps`.
  - **Rewards claim (rewards-payout.ts nuevo)**: en live mode el claim es un payout REAL on-chain — la tesorería (`TREASURY_KEYPAIR_BASE64`) transfiere USDC SPL (mint por `SOLANA_REWARDS_USDC_MINT`, default mainnet USDC) a la ATA del usuario. Requiere wallet Solana vinculada a la cuenta (verificada por identidad firmada; sin destino→400, wallet ajena→403). Idempotente por batch (`payoutSessionId` determinista + marcador `rewards-payout-batch:`→ALREADY_CLAIMED 409); journal pre-broadcast + flip del ledger a CLAIMED en la misma transacción SQLite; recovery `GET /api/rewards/claim/:sessionId` resuelve contra RPC sin re-enviar (prepared/unknown/pending/confirmed/failed). `rewardsPayoutFromEnv` adapta Connection a `PayoutSolanaLike` (getSignatureStatus desenvuelve value). Mock mode conserva el claim interno etiquetado. UI rewards.js: botón "CLAIM ON-CHAIN (USDC)", pide wallet destino con prompt y muestra link de solscan.
  - Tests: 372 app (+14: exactitud de monto/decimales en tx real, idempotencia por batch, orden journal→broadcast, recovery RPC sin re-send, keyless refusal, BAD_ADDRESS, STALE_QUOTE, drift, session binding, env kill switch arable sin redeploy, claim live exige wallet vinculada) + 101 core, typecheck/build OK, sintaxis site OK.
  - Para activar en Fly: `fly secrets set POOL_EXECUTION_ENABLED=1 LAUNCH_POOL_KEYPAIR_BASE64=<pool keypair> TREASURY_KEYPAIR_BASE64=<treasury> SOLANA_RPC_URL=<helius>` y fondear la pool (ATA token+USDC) y la tesorería (USDC + SOL para fees). El flujo de wallet vinculada ya existía (`/api/wallet/link`).

- 2026-09-22 (ensayo devnet LaunchLab EJECUTADO y CERTIFICADO on-chain): fondeada wallet `GweaTTSixLxqoL9a1bk4sjJrvbtSpyw4VFAsC7GvMKYK` con 1 SOL en devnet. Ejecutado `scripts/devnet-launchlab-swap.mjs` de extremo a extremo contra el programa LaunchLab devnet (`DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6`) y pool activa `HxQjuW4DRkUAmV9esW4qxyT2yT2XKJ1t3uk1fxq4vvBV` (mint `7RKVmzsgvXNNfBRCPnyktkyiMf571JCJ45YdkDgetTnQ`).
  - **Compra (BUY)**: 0.05 SOL convertidos a 1,217,787,460 base units. TX confirmada: `Cq7qfkC7s8Us2h3GJP8nQqXtTwv4m4rnMHZG696P7ZffUQxPHnL931rdQXXzAP39VvknBbxABXmyhZ9WrzzbDmd`.
  - **Venta (SELL)**: 1,217,787,460 base units vendidos de vuelta a la curva por 0.04925 SOL netos. TX confirmada: `39RjA6KeoME82gcfbCXSuJAeFXQJMofetSDguGpzdgBEnZPvWhzSJuLh2R2WFPhEpFv6muwuz3GJDqYg5oZyPsxY`.
  - **Balance token final**: 0 exacto.
  - **Veredicto**: PASS total. Probado que la derivación de cuentas, el layout de instrucciones (18 accounts, discriminadores de Anchor `buyExactIn` / `sellExactIn`), wrapping wSOL y la matemática de la curva son 100% compatibles con la implementación viva de Raydium LaunchLab. 358 tests app verdes.
- 2026-09-22 (ensayo devnet preparado; testnet descartado con evidencia): verificado on-chain que **testnet NO tiene ningún programa de Raydium** (CPMM/LaunchLab no existen ahí) y devnet SÍ tiene LaunchLab (`DRay6fNdQ5J82H7xV6uq2aV3mNrUZ1J4PgSKsWgptcm6`, executable) pero NO CPMM — el swap post-graduación solo puede certificarse en mainnet, la curva LaunchLab sí puede ensayarse en devnet. Creado `packages/app/scripts/devnet-launchlab-swap.mjs`: descubre curva viva en devnet vía GPA (dataSize=LaunchpadPool.span=429, status 0, mintProgramFlag 0), keypair fresca + airdrop, quote con `Curve.buyExactIn/sellExactIn` (misma matemática que la API), compra REAL (wrap SOL + buyExactIn con 19 cuentas idénticas al motor pero con DEV_LAB) y venta REAL del balance completo, con verificación de balance final 0 y enlaces a explorer. Estado: BLOQUEADO SOLO POR FINANCIACIÓN — el faucet de devnet público está agotado/limitado (429 desde IP doméstica y desde Fly; Ankr exige auth; Helius sin key). Para ejecutar: `node scripts/devnet-launchlab-swap.mjs` (retry cuando el faucet revive) o `DEVNET_SECRET_B64=<keypair devnet con SOL> node scripts/devnet-launchlab-swap.mjs`, o RPC propio con faucet (Helius/QuickNode devnet) vía `SOLANA_RPC_URL`. Keypair de prueba ya generada (guardada en %TEMP% de la sesión): `7StxiptVmmfQwhXbLGptzxk6hCwCKPRzifuTF1qYt3LA` sin fondos aún. El programa devnet LAB usa PDAs distintas pero mismas 19 cuentas que el motor — el script replica swapKeys/launchpadSwapData byte a byte cambiando solo el programId.
- 2026-09-22 (publicación y verificación): commit `3572b99` (CPMM swap UI + metadata + resiliencia + PWA) y `d92efd9` en la rama codex/market-data-20260915. API redesplegada en Fly (`deployment-01M348KV3Z81H03PBQTPDZ9RRR` y luego la del fix de key), máquina 84ed23ea636178 healthy. Web publicada en gh-pages `bbe8a34` versión 20260922-2 (copia de publicación clonada en $TEMP, CNAME y .nojekyll conservados; solo 8 ficheros cambiaron de verdad, el resto era ruido CRLF). Verificación pública: health 200, `/api/market/search` y `/api/market/pools` 200 con source=geckoterminal LIVE (429 intermitente), GET /api/token-metadata/:id responde 404 con id inexistente (ruta viva), sw.js/install.js/manifest 200, index sirve v=20260922-2.
- Hallazgo 2026-09-22: CoinGecko onchain (`/api/v3/onchain/*`) devuelve **401 a IPs de datacenter sin API key** (desde red residencial responde 200, por eso los tests offline pasaban); GeckoTerminal sigue limitando la IP compartida de Fly con 429 intermitente. Solución aplicada: `MarketDataService` lee `COINGECKO_API_KEY` y envía el header `x-cg-demo-api-key` SOLO al proveedor coingecko (test incluido; 358 tests app). Pendiente para activar el fallback server-side: crear la key gratuita en coingecko.com y `fly secrets set COINGECKO_API_KEY=...`. Mientras tanto el usuario final queda cubierto por el respaldo del navegador (`public-market.js`, GeckoTerminal directo desde su IP).

- 2026-09-22 (UI del swap CPMM en la ficha del graduado): en `markets.js`, sección "🏊 Pool CPMM de Raydium" dentro de la ficha LaunchLab cuando `statusRaw===2` (curva cerrada y migrada): precio SOL/token, reservas reales, panel comprar/vender con presets de cotización en vivo (debounce 350ms) y confirmación antes de Phantom. `api.js` con `getCpmmState/quoteCpmm/prepareCpmmSwap/submitCpmmSwap/getCpmmSession/getCpmmActivity`. `submitCpmmTrade` guarda `inusaur.cpmm.pending` antes de submit y, si falla la conexión tras firmar, consulta el endpoint de recuperación `GET /api/cpmm/sessions/:id` y muestra estado/firma. Los endpoints prepare/submit siguen detrás de `CPMM_EXECUTION_ENABLED`; la sección muestra la pool real aunque el flag esté apagado (solo el submit dará 503 honesto). Importmap bump v=20260922-2. 357 tests app (+2 UI), typecheck OK, sintaxis de markets.js verificada.
- 2026-09-22 (CPMM endurecido, listo para activar con una condición): guard de mints Token-2022 en `launch-cpmm.ts` vía `MintGuard` inyectable (`splTokenMintGuard()` con `unpackMint`/`getTransferFeeConfig` del SDK oficial): rechaza transfer-fee activo (older/newer bps>0), mint authority viva (oferta mutable) y freeze authority; se consulta al decodificar (owner real de la cuenta del mint decide si aplica), y de nuevo en prepare/submit (cambio de estado entre fases). Fixtures Token-2022 en tests construidos a mano y validados contra unpackMint real (TLV u16/u16 en offset 165+1, NO u32/u32 en 82; quirk del SDK: byte AccountType en ACCOUNT_SIZE=165). Nuevo endpoint durable `GET /api/cpmm/sessions/:id` (auth, kind=cpmm-swap): prepared/not_broadcast sin journal, y con journal verificado contra RPC (unknown/pending/failed/confirmed + `recordLaunchLabTrade` idempotente) — espejo del contrato LaunchLab. E2E completo sobre HTTP real: flag off→503, flag on→prepare→firma browser-style→submit→ledger→recuperación idempotente→replay 409. 355 tests app (+5), 101 core, typecheck/build OK. Certificación mainnet re-ejecutada tras los cambios: PASS (diff precio 0,26%, round-trip 0,51%). Para activar: fijar `CPMM_EXECUTION_ENABLED=1` + `EXECUTION_ENABLED` y simular swap con wallet financiada (`USER_PUBKEY`) en el script.
- 2026-09-22 (CPMM certificado con mainnet, ORDEN DE MINTS CORREGIDO): evidencia on-chain (pool real de migración `Q2sPHPdUWFMg7M7wwrQKLrn619cAucfRsmhVJffodSp`, token `Dz9mQ9Nz…`, LaunchLab status=2/migrateType=1) demuestra que LaunchLab migra a CPMM con **WSOL como mintA de la pool** (PDA derivada con WSOL primero), al contrario de lo que asumía `launch-cpmm.ts`. Corregido: `decodePool` intenta ambas derivaciones y decide la cuenta real, `effectiveReserves`/precio/`state` mapean por orientación (API sigue exponiendo mintA=token, mintB=WSOL), prepare/submit usan tokenProgram/solProgram/tokenVault/solVault por orientación. Fixture de tests alineado al orden real (mintDecimalA=9). `scripts/certify-cpmm.mjs` (read-only, sin firmas/broadcast): descubre pools por GPA con config de migración + WSOL, lee estado vía `decodePool`, cotiza compra/venta, cruza precio contra Jupiter y simula sin firmas con `USER_PUBKEY`. Resultado con pool real: precio vs Jupiter diff 0,12–0,45% PASS, round-trip 0,5% (dos legs × fee 0,25%), reservas positivas. 19 tests CPMM + 350 app + 101 core, typecheck/build OK. PWA (manifest+icons+sw network-only+install.js) y flujo de creación con foto+redes ya cableados de la sesión anterior. `CPMM_EXECUTION_ENABLED` sigue en 0: falta validación Token-2022 en swap (ata fee/side B), simular con wallet financiada real y decidir política de sesiones durable antes de activar.
- 2026-09-21 (continuación, web 20260921-5): publicado gh-pages `3f0f62d`, workflow `35633999414` success. LaunchLab cerrado ofrece abrir el terminal por mint exacto para consultar pools/rutas existentes. Estado RPC prevalece sobre metadatos guardados; total vendido normalizado por decimales; cerrar ficha invalida respuestas pendientes y cancela polling. 344 tests app y build/typecheck pasan. Smoke público: Markets carga, método nuevo presente, 1280px y 390px sin overflow, API health live. No había launches registrados para comprobar graduación real; recorrido cubierto con fixtures explícitas en tests, sin mover fondos.
- Nota RPC (2026-09-22): el RPC público principal limita getProgramAccounts (429/INVALID_PARAMS según carga); GeckoTerminal también en 429. Para certificación usar `SOLANA_RPC_URL` propio o reintentar; el script cae a candidate-by-mint explícito y no inventa datos.
- Trabajo CPMM local conservado (NO publicado en API): corregidas ATAs mediante helper SPL oficial, flags del fee payer tras serialización, precio con decimales, bit de pausa swap, openTime, rango u64 y creator fee apagada. 19 tests CPMM pasan; se corrigieron también fixtures erróneas. Nuevos endpoints prepare/submit quedan desactivados por defecto (`CPMM_EXECUTION_ENABLED` requiere 1). NO activar todavía: faltan certificación con pool real/config de migración y orden de mints, validación completa de cuentas/Token-2022, simulación y recuperación durable de sesiones. La cobertura offline no prueba operación mainnet. Referencia revisada: https://github.com/raydium-io/raydium-cp-swap/blob/master/programs/cp-swap/src/states/pool.rs .
- Bloqueo operativo observado en el smoke p?blico: `/api/market/pools` y `/api/market/trades` devolvieron 503; respaldo GeckoTerminal en navegador fall? por CORS. No declarar disponibilidad continua del mercado. Prioridad siguiente: resolver proveedor/cuotas de datos y verificar acceso de usuarios de extremo a extremo. No se cambiaron credenciales, c?digos de acceso ni se enviaron transacciones.

- 2026-09-21: factory endurecida y publicada en Fly `deployment-01M3175V4YWVY62PGHXT3XQBD7`; web `cbe209f`, versión 20260921-1. Corregida derivación ATA (mint, owner); plan canónico rechaza cambios en destinos/metadatos; importes exigen enteros seguros; lotes de 4 probados con 18 receptores y límite 1232 bytes; mint sin autoridad de freeze. Bloqueo SQL compare-and-set planned→executing; no permite replanificar ejecuciones en curso. Journal durable ANTES del broadcast con firma calculada, tx firmada pública, blockhash/altura y confirmación. RPC debe devolver firma exacta y value.err=null. Fallos ambiguos quedan recovery_required, sin remint automático. Curva/claims bloqueados durante emisión/revisión y tras graduación. Rutas ya no sobrescriben journal; plan.launchId debe coincidir con URL. 299 tests, build y typecheck pasan. Recuperación manual/reanudación automática, liquidez y curva real aún pendientes; no se configuraron firmantes ni se movieron fondos.
- UI 2026-09-21: ficha de launch accesible sin login (lectura), sección Estado on-chain, operaciones de curva ocultas si distribución bloqueada y navegación al terminal por mint para tokens realmente emitidos (sin presumir liquidez). Producción no tenía lanzamientos para un recorrido con datos existentes. Preguntado al usuario si prefiere protocolo Solana existente o programa propio para la fase de liquidez; pendiente de respuesta.

- Verificación pública final 2026-09-20: GitHub Pages `2fcab5e` built; dominio sirve módulos 20260920-2, tema y radar. E2E ejecutado contra https://inusaur.online con BONK: velas y marcadores reales, filtro de wallet, toggle, móvil 390px sin overflow, reduced motion y cancelación al cerrar aprobados. API health live. Los 503 de GeckoTerminal desde Fly se recuperan por el respaldo público del navegador; no son errores de JavaScript ni garantía de disponibilidad futura.

- 2026-09-20 (publicación): portada nueva confirmada HTTP 200 en el dominio público y navegador. GeckoTerminal desde Fly devuelve HTTP 429 (confirmado por lectura SSH); no hay clave CoinGecko configurada. Release web `2fcab5e` / módulos `20260920-2` añade respaldo público de lectura desde navegador, sin credenciales/referrer, caché/coalescing, timeout y cooldown de 60 s tras 429. 295 tests app y typecheck pasan. Las cuotas siguen siendo una limitación operativa; para garantizar capacidad hace falta un proveedor con presupuesto/cuota asignada. No afirmar cobertura universal ni actividad completa de wallets.

- 2026-09-20: usuario encarga publicar mejoras directamente en inusaur.online; prioridad a cambios visibles, estética futurista, partículas y traders sobre gráficos. Publicación web gh-pages `18b80e7`, versión `20260920-1`; API Fly `deployment-01M303H6D8BCCB2R5KMG5ZZZT2`. Radar de wallets con trades observados de GeckoTerminal (muestra máxima de 300), orientación por contrato, filtro de wallet, flechas por vela, enlaces a transacciones, caché explícita y actualización cada 30 s. Gráfico refresca cada 60 s visible; cierre cancela polling/respuestas pendientes; precisión para microprecios. Portada y terminal azul noche con partículas y reduced-motion. 291 tests app y build pasan; E2E local real de BONK valida filtro, marcadores, móvil sin overflow y cierre. Verificación pública pendiente al escribir esta entrada.
- 2026-09-21 (LaunchLab real): integración completa de Raydium LaunchLab en `packages/app/src/trading/launch-raydium.ts` + endpoints `/api/launchlab/*` + sub-pestaña UI "🧪 LaunchLab on-chain" en markets.js. Curva REAL on-chain (estado leído con decoders del SDK oficial v2 fijado 0.2.72-alpha, quotes con matemática exacta, graduación a CPMM automática por crank de Raydium). Self-custody estricto: sesiones durables exactly-once reutilizando self_custody_sessions (id = hash del mensaje sin firmar), submit re-verifica la instrucción firmada byte a byte contra el payload almacenado (discriminador, 18/19 cuentas, minOut, owner) y rechaza firmas ajenas. Creación sin custodia: mint keypair generada/firmada en el navegador (firma ed25519 local sobre serializeMessage + reconciliación post-wallet porque algunos providers descartan firmas previas), el servidor verifica createAccount+initializeV2 (config canónica, PDAs, metadata decodificada) y exige firmas creator+mint exactas antes de broadcast. TRAMPAS descubiertas: (1) Transaction.partialSign falla con 'unknown signer' sobre txs parciales del servidor — firmar directamente con nacl/noble sobre serializeMessage() y usar addSignature; (2) web3.js marka isSigner=true en TODAS las ocurrencias de una pubkey firmada al serializar/parsing — verificar el CONJUNTO distinto de firmantes; (3) el layout real de createAccount es 52 bytes (space@12, programId@20); (4) sellExactIn del SDK ya devuelve monto neto de fee. 17 tests offline con encoders reales del SDK (PoolState encode/decode roundtrip); 316 app total, typecheck/build OK.
- Auditoría 2026-09-20: AMM de launchpad NO es autónomo ni no custodial; reservas controladas por operador. Prepare/submit bloqueados 503 por vulnerabilidades: importes de salida del cliente, instrucciones adicionales, falta de sesiones durables y concurrencia de reservas, Token-2022/USDC usan programa único y confirmación ignorada. Corregidos snapshot async/serialización bigint y precio USDC (factor 1e6). Spot self-custody sigue separado. Factory requiere revisión adicional: ATA derivada con mint/owner intercambiados, confirmación no inspeccionada y recuperación tras ejecución parcial pendiente. No declarar blockchain 100% funcional ni habilitar factory/AMM con fondos hasta resolverlo.

- Workspace: `C:/Users/Usuario/Desktop/trenches`.
- Producto: terminal de descubrimiento, tesis y trading on-chain; objetivo mobile-first. Idioma de colaboración: español claro y conciso.
- Instrucción vigente (2026-09-16): auditar antes de cambiar, conservar trabajo válido, implementar por fases y probar. No confundir UI, cotización, envío, confirmación y reconciliación.
- Documentos activos: `MASTER_PLAN.md`, `IMPLEMENTATION_STATUS.md`, `ARCHITECTURE.md`. Los planes previos quedan como historial.
- Frontend: `site/`; API/dominio: `packages/app`; comunidad: `packages/core`; bot separado: `saur-bot`.
- No mocks silenciosos ni valores inventados. Activos = cadena + contrato. Información desconocida = desconocida.
- No enviar fondos, desplegar producción, cambiar infraestructura sensible, borrar datos/secretos ni ejecutar migraciones destructivas sin autorización explícita.
- Base financiera aún incompleta. Ejecución live bloqueada hasta evidencia integral. Una suite verde no prueba trading real.
- No hay integración de memoria global disponible; este archivo es memoria versionable local del proyecto.
- Avance local 2026-09-16: catálogo/importación/cursor/worker y filtros de tres columnas; terminal adaptable con historial. 213 tests app, typecheck/build pasan; E2E filtros/scroll/foco/atrás pasa. Lint y fases financieras posteriores pendientes. Evidencia en `docs/audit/2026-09-16-progress.md`.
- Catálogo/API y frontend publicados a petición del usuario; verificación pública final 2026-09-17. Worker incluido en imagen pero no habilitado como servicio continuo. No tratar pools observados como cobertura universal ni tests como prueba de swaps reales.
- Usuario pidió retirar Polygon, Arbitrum y Monad: excluidas de redes disponibles y consultas; no reintroducirlas. Web 654c5fd; API deployment-01M2NCMAAWZTAHVYMQ6W44KZ85. 216 tests, typecheck/build y smoke público aprobados.
- 2026-09-17 (tarde): usuario pidió modo real y apertura a usuarios. Activada ejecución self-custody en solana/ethereum/base: prepare/submit con sesión exactly-once, propiedad de wallet exigida, liquidación solo con recibo verificado (deltas SPL y logs Transfer), reconciliador periódico 15 s en vivo. UI habilita el botón solo con capacidad LIVE. Desplegado con autorización: Fly máquina 84ed23ea636178, gh-pages 4f13744; JUPITER_API_KEY y ADMIN_SECRET añadidos a Fly. Acceso beta con ACCESS_CODES se mantiene. Smoke público aprobado. Evidencia: docs/audit/2026-09-17-self-custody.md.
- 2026-09-17 (hardening): kill switch durable (app_settings + /api/admin/execution, EXECUTION_ENABLED=0 como default por entorno), límite diario de exposición por usuario (1.000 USDC/día por defecto, EXECUTION_DAILY_LIMIT_USDC, cuenta patas USDC persistidas por sesión), POST /api/wallet/link para vincular wallet a cuenta Google/X sin rotar clave, fee on-chain EVM 0x v2 implementado y apagado (EVM_SWAP_FEE_RECIPIENT/BPS). Fix: replay de Idempotency-Key en prepare daba 500, ahora idempotente. E2E en producción con wallets efímeras: link real ed25519/EIP-191, prepare real Solana/Base, submit pending, replay 409. 246 tests. gh-pages bf5c75e v20260917-3.
- 2026-09-17 (fees + operador): fee Solana Jupiter implementado y apagado (SOLANA_PLATFORM_FEE_OWNER/BPS; platformFeeBps en quote, feeAccount swap-level = ATA del fee owner para el mint de salida, derivada en runtime con @solana/spl-token; SOLANA_PLATFORM_FEE_ACCOUNT como override estático). Contabilidad de fill unificada: feeAmount/feeToken (fee=net·bps/(10000−bps) reconstruido del delta verificado; se cuenta en USDC solo si la pata del fee es USDC). Panel de operador en la web (modal discreto, secreto admin por header; toggle + límite + reconciliación manual). 250 tests, typecheck/build OK. gh-pages 911ab1c v20260917-4. E2E en producción re-aprobado tras el deploy.
- 2026-09-17 (fees activados): fee on-chain 0,3% LIVE en producción — EVM (0x swapFeeRecipient=tesorería del usuario, 30 bps) y Solana (Jupiter platformFeeBps=30, feeAccount=ATA del fee owner por mint). CRÍTICO: Jupiter rechaza /swap con 400 si el quote lleva platformFeeBps sin feeAccount en /swap; por eso la existencia de la ATA se verifica por RPC (getAccountInfo, caché 5 min) ANTES de cotizar y si no existe el fee se omite para ese mint sin romper el swap. El bps efectivo se persiste en la sesión (request.platformFeeBps) y la reconciliación usa ese valor persistido, nunca el env vivo. 251 tests. E2E en producción re-aprobado ×2.
- 2026-09-17 (ATAs de fee): script packages/app/scripts/solana-fee-atas.mjs (--check read-only / --create idempotente con SOLANA_FEE_SECRET local, nunca impresa). Check del top-60 real del catálogo + USDC/wSOL: 1 ATA existe, 60 faltan, ~0.09 SOL de renta. Tesorería 48S2…bFy8 con 0.0009 SOL: pendiente que el usuario fondee ~0.15 SOL y ejecute --create; después el fee Solana fluye sin redeploy (caché del motor expira en 5 min).
- 2026-09-19 (AMM post-graduación): mercado secundario NO custodial para tokens graduados — módulo launch-amm.ts. La pool es un keypair operador (LAUNCH_POOL_KEYPAIR_BASE64 en env; servidor keyless por defecto) dueño de dos vaults (ATA del token graduado + ATA USDC). Reservas NUNCA en DB: se leen de los balances SPL reales en cada quote/snapshot (parseSplAccount: mint@0, owner@32, amount u64 LE@64). Matemática constant-product con fee 0.3% en el input (SWAP_FEE_BPS=30) y minOut obligatorio (DEFAULT_SLIPPAGE_BPS=100, clamped 0-5000). Flujo swap: POST /pool/swap/prepare construye tx SIN firmar (usuario = fee payer + primer firmante; slot de la pool vacío) → usuario firma en Phantom (signTransaction, co-sign) → POST /pool/swap/submit verifica forma EXACTA (fee payer = usuario, usuario firmó, pool NO firmó por cliente, exactamente 2 legs transferChecked con sources/dests/authorities/amounts/decimals preparados) → pool co-firma con partialSign (sign() resetearía firmas) → broadcast + confirm + historial en pool_swaps. Endpoints: GET /api/launches/:id/pool (snapshot), POST .../pool/quote, POST .../pool/swap/prepare + .../swap/submit (auth, live mode, kill switch, wallet linkeada requerida), POST .../pool (admin: solo lanzamientos graduados_on_chain con mint real; NO mueve fondos — el operador fondea los vaults aparte). DB: tablas launch_pools (registro) + pool_swaps (historial). UI: sección "🔄 Mercado" en la ficha del graduado (precio de pool, reservas reales) + swap con Phantom. Importmap v=20260919-4. 284 tests app (+15 AMM: math, parsing, vaults, forma de tx sin firmar, co-firma verificada con firmantes reales, NOT_USER_SIGNED, pool-sig-provista-por-cliente rechazada, amounts alterados rechazados, fee payer ajeno rechazado, servidor keyless rechaza), core 101, typecheck/build OK. Siguiente: guardar amountOut real desde el receipt on-chain en lugar del floor, UI de swap dedicada con slippage ajustable.
- 2026-09-19 (factory on-chain): graduación real implementada — módulo launch-factory.ts con contrato de honestidad v1: la curva nunca custodió USDC, así que la factory ACUÑA el token SPL real (Token-2022, 0 decimales = unidades enteras del ledger, sin conversión) y lo reparte directamente a las ATAs de las wallets con claim firmado en launch_claims; NO inventa liquidez (eso es decisión explícita del operador). Flujo: plan() genera plan + dryRun inspectable + persiste estado planned; execute(plan, treasury) re-verifica contra la distribución ACTUAL de claims (STALE_PLAN si cambió), crea el mint, hace mintTo por lotes de 8 (TRANSFER_BATCH_SIZE) y REVOCO la autoridad de mint — oferta fija para siempre. Puerto SolanaLike inyectable (Connection lo satisface; tests offline con stub). El keypair del mint vive solo en la sesión que planificó (recentPlans en memoria): tras reinicio hay que regenerar y rever el plan. El servidor es keyless por defecto: TREASURY_KEYPAIR_BASE64 opcional; kill switch de ejecución aplica. Endpoints admin: GET /api/launches/:id/factory, POST .../factory/plan, POST .../factory/execute (x-admin-secret). DB: columnas mint_address/factory_status/factory_result/graduated_on_chain en launches; formatLaunchPublic expone graduatedOnChain/mintAddress reales; UI muestra badge "Graduado on-chain" con link a Solscan y oculta la sección de reclamo tras la graduación. Importmap v=20260919-3. 269 tests app (+7: plan exacto con exclusión EVM, batches de 8, doble graduación rechazada, ejecución completa con stub Solana verificando create/mintTo/revoke + persistencia, plan vencido rechazado sin marcar executing, reinicio de proceso rechazado, round-trip JSON), core 101, typecheck/build OK. Siguiente: LP opcional operado a mano o integración de mercado secundario.
- 2026-09-19 (claims): reclamo de tokens por wallet registrado con firma — prerequisito de la factory on-chain. Tabla launch_claims con UNIQUE(user_id, launch_id) y UNIQUE(launch_id, wallet_address): una wallet no puede absorber holdings de varias cuentas del mismo launch; re-registrar actualiza, nunca duplica. registerClaim valida formato (base58 32 bytes / 0x-40 hex), verifica firma ed25519 (Phantom) o EIP-191 (MetaMask) con los mismos helpers de verify-login y solo entonces persiste. Snapshot de distribución derivado del ledger neto (compras−ventas) por wallet registrada; wallets sin reclamo quedan excluidas hasta que su dueño registre — el destino del pago es opt-in, los tokens netos son derecho del holder. Endpoints: POST/GET /api/launches/:id/claim (auth) y GET /api/launches/:id/distribution (admin). UI: sección de reclamo en la ficha con firma gratuita (challenge existente, sin gas), estado honesto de wallet registrada y aviso de que la curva sigue siendo simulada. 262 tests app (+3: firma inválida sin escritura, idempotencia + colisión de wallet, distribución neta EVM excluyendo no-registrados), core 101, typecheck/build OK. Siguiente: factory on-chain (deploy de token + migración de liquidez al graduarse) consumiendo launch_claims.
- 2026-09-19 (launchpad): corregido exploit P0 — sellTokens pagaba USDC sin verificar holdings; ahora existe tabla launch_trades (compras/ventas brutas por usuario) y la venta se cubre con tokens netos, todo dentro de db.launchTransaction (atómico). Curva con floor anti-dust (0.1 USDC), market cap corregido (factor 1e6), buyers_count cuenta filas reales, validación de creación (símbolo A-Z0-9 2-10, supply ≤ 1e15, nombre 2-64) aplicada ANTES de cobrar la fee. Graduación marcada graduation_simulated=1 y SIN txHash falso. Endpoints nuevos: GET /api/launches/:id, /position, /quote (estimación sin mutar) y /activity (ledger real). UI: fin de los prompt() nativos — modal de creación con validación, ficha de token con posición (PnL, coste medio), presets, cotización en vivo con debounce y auto-refresh 8 s; etiqueta honesta "curva simulada". 259 tests app (+8), core 101, typecheck/build OK. Pendiente: factory on-chain real (graduación a DEX).
